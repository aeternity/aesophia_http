contract Remote =
  entrypoint f : () => unit
contract Test =
  record r = { x : int, y : string, z : map(int, int) }
  datatype d('a) = One('a, 'a) | Two
  entrypoint a : () => int * bool * string * unit
  entrypoint b : () => d(int) * d(int)
  entrypoint c : () => r
  entrypoint d : () => bytes(2) * bytes(14) * bytes(32) * bytes(65)
  entrypoint e : () => oracle(int, int) * oracle_query(int, int) * Remote * address
  entrypoint f : () => list(int)
